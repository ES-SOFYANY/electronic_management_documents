'use strict';

const configTest = {
    mongo: {
        uri: 'mongodb://localhost/documents-test',
        options: {
            useNewUrlParser: true
        }
    }
};

export default configTest;
